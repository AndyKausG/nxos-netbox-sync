# Python 3.10+ required (pyATS >= 24.x dropped Python 3.9 support)
# On AlmaLinux 9: dnf install python3.10 or use the container image

# pyATS + Genie (monolithic install — includes pyats.contrib for testbed generation)
pyats[full]>=24.0

# NetBox client
pynetbox>=7.0.0

# Templating
Jinja2>=3.0.0

# Utilities
netaddr>=0.8.0
PyYAML>=6.0
requests>=2.28.0
prettytable>=3.0.0
xmltodict>=0.13.0
psutil>=5.9.0
